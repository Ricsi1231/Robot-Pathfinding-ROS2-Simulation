# Models

Optional reusable SDF models / meshes for the simulation world. Drop model
directories here; they are installed to `share/robot_pathfinding_description/models`
by `setup.py` and can be referenced from `worlds/grid_world.sdf`.
